<h1>Write a New Page</h1>

<%= form_for [@adventure, @page] do |f| %>
    <%= f.text_field :name, placeholder: "Page Title" %><br>
    <%= f.text_area :text, placeholder: "Story Text" %><br>
    <%= f.submit "Create Page" %>
<% end %>