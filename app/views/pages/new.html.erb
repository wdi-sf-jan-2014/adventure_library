<h1><%= @adventure.title %> </h1>
<br>
<p><%= @adventure.author %> </p>

<% @adventure.pages.each do |pages| %>
<%= pages.name %><br>
<%= simple_format(linkify_page(pages)) %><br><br>
<% end %>
<br>

Add A New Page
<%= form_for [@adventure, @adventure.pages.build] do |f| %>
  <%= f.text_field :name, placeholder: "Name" %><br>
  <%= f.text_area :text, placeholder: "Text" %>
  <%= f.submit %>
  <% end %>
