
All Adventures:<br>

<%for i in @adventures %>
	<% if i.pages.find_by(name: "start") %>
	 <%=link_to i.title, adventure_page_path(i,i.pages.find_by(name: "start"))%><br>
	<%else%>
	 <%= i.title %>
	<%end%>
<%end%>
