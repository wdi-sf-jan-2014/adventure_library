<h1>Choose an Adventure...</h1>
<% unless @adventures.empty? %>
  <% @adventures.each do |adventure| %>
    <li><%=link_to(adventure.title, adventure_page_path(adventure, adventure.pages.find_by(name: "start"))) %> by <%= adventure.author %></li>
  <% end %>
<% end %>