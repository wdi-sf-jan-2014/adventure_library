<div>
<h4><%= adventure.title %></h4>id: <%= adventure.id %>
<h6>by <%= adventure.author %></h6>
<% if  adventure.pages.first %>
  <a href="<%= adventure_page_path(adventure.id, adventure.pages.find_by_name('start').id ) if   adventure.pages.find_by_name('start') %>">Click here to start!</a></div>
<% else %>
  error: this adventure doesn't have any pages id: <%= adventure.id %>
<% end %>